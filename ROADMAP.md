# MoneyLeft 待辦與規劃

> 這份文件是「記下來、還沒做」的清單。實作時從這裡拿項目，做完把該段標成 ✅ 並寫上版本號。

目前版本：**v2.0**（移除 iCloud + 語音記帳 + 備份還原 + 發票 QR + 通知 + App 鎖）

---

## 規則：每次修改都要更新版本號 ⬅️ 必守

下載新 ipa 卻不確定有沒有更新到，就是因為版本號沒動。往後一律照這個來：

| 項目 | 規則 |
|---|---|
| `MARKETING_VERSION` | 功能有變就 +0.1（1.1 → 1.2）；大改版 +1.0。寫在 `project.yml` |
| `CURRENT_PROJECT_VERSION`（build） | CI 自動帶入 `github.run_number`，每次建置一定不同 |
| ipa 檔名 | 改成 `MoneyLeft-v1.2-build17-unsigned.ipa`，下載就看得出哪一版 |
| Artifact 名稱 | 同上帶版本號 |
| App 內顯示 | 「更多」頁最下面已經有 `v{version} ({build})`，安裝後第一件事就是看這裡對版本 |
| 每次交付 | 我回報時直接寫「這是 v1.2 build 17」，你對得起來 |

實作方式（下次動程式時一起做）：
- `project.yml` 的 `CURRENT_PROJECT_VERSION` 改由 CI 覆寫：`xcodebuild ... CURRENT_PROJECT_VERSION=${{ github.run_number }}`
- workflow 讀 `MARKETING_VERSION` 組出檔名
- 「更多」頁的版本字樣放大一點、可長按複製

---

## 1. ✅ 完全移除 iCloud 同步（v2.0 完成）

決定：**不要雲端同步，資料只留在本機。** 要拔得乾淨，不是只把開關藏起來。

### 要改的地方

| 檔案 | 動作 |
|---|---|
| `Shared/Persistence.swift` | 刪掉 `makeCloudContainer()`、`SyncStatus`、閃退旗標那套；只留單一本機容器 |
| `Shared/CloudSyncCoordinator.swift` | 整個檔案刪除（`StoreCopier` 保留，改名 `BackupCopier`，之後備份/還原會用到） |
| `Shared/AppSettings.swift` | 刪 `cloudSyncEnabled`、`cloudLaunchInProgress`、`cloudFailureNote`，並在啟動時 `removeObject` 清掉舊 key |
| `Shared/AppGroup.swift` | 刪 `cloudKitContainerID` |
| `MoneyLeft/MoneyLeft.entitlements` | 刪 iCloud 三個 key 與 `aps-environment`，只留 App Group |
| `MoneyLeftWidget/MoneyLeftWidget.entitlements` | 同上 |
| `MoneyLeft/Views/SettingsView.swift` | 刪整張 iCloud 卡，改成「資料儲存」卡：顯示資料庫大小、筆數、備份入口 |
| `PrivacyView` | 改寫成「資料 100% 只在這台裝置，App 沒有任何連網能力」 |
| `README.md` | 移除 §8 相關說明，改寫成本機專用 |

### 資料搬遷（重要，別讓紀錄不見）

啟動時若偵測到舊的 `MoneyLeft-cloud.store` 存在而且有資料：
1. 把裡面的紀錄複製回 `MoneyLeft.store`（只在本機那份是空的、或使用者確認後才做）
2. 完成後把 cloud store 檔案改名成 `.bak` 保留一次，確認沒事再刪
3. 跑完顯示一次提示：「已把 N 筆紀錄搬回本機」

### 順便

- 拔掉 iCloud 後，entitlement 變單純，自簽更不容易出事
- 「不連網」變成賣點，可以在隱私頁直接寫死

---

## 2. ✅ 語音記帳（本機、無 AI）（v2.0 完成）

### 使用流程

1. 入口：底部「＋記一筆」選單多一顆 **🎙 語音記帳**；首頁長按 ＋ 也可直接進
2. 進入全螢幕錄音畫面：波形動畫 + 即時逐字稿
3. 講完後 **偵測到 1.2 秒靜音自動停止**（也可手動按停止）
4. 解析逐字稿 → **自動打開記帳確認 Sheet**，金額／分類／子分類／情緒／日期／備註全部先填好
5. 使用者看一眼、要改就改，按送出才寫入
6. 一句話多筆也支援：「早餐五十然後咖啡六十」→ 產生兩筆，逐筆確認

### 技術

| 項目 | 選擇 |
|---|---|
| 語音辨識 | `Speech` framework，`SFSpeechRecognizer(locale: zh-TW)` + `SFSpeechAudioBufferRecognitionRequest` |
| 離線 | `requiresOnDeviceRecognition = true`，並先檢查 `supportsOnDeviceRecognition` |
| 錄音 | `AVAudioEngine` 取 buffer，不落地存檔 |
| 權限 | `NSSpeechRecognitionUsageDescription` + `NSMicrophoneUsageDescription` |
| 解析 | 純規則 + 詞庫，**不含任何 AI/連網** |

⚠️ 要注意的現實問題：中文的 **裝置端聽寫需要使用者先下載語言包**（設定 → 一般 → 鍵盤 → 啟用聽寫）。
若 `supportsOnDeviceRecognition == false`，我們**不要**偷偷改用線上辨識（那會把語音送到 Apple 伺服器，違反這個 App 的原則）。
正確做法：跳提示告訴使用者怎麼開啟離線聽寫，並提供「用鍵盤的麥克風輸入」當替代。

### 解析器設計（重點：講不標準也要聽得懂）

#### A. 數字正規化
- 中文數字 → 阿拉伯數字：一二三四五六七八九十百千萬、兩、廿、半
- 口語簡寫：「一百二」=120、「兩百五」=250、「三塊半」=3.5、「五十塊」=50
- 單位詞白名單：塊、元、圓、摳、塊錢、台幣、NT、NTD
- 容錯：ASR 常把金額聽成同音字，先過同音表再轉數字

#### B. 分類詞庫（台灣 + 軍中用語，離線 JSON）
每個分類配一組關鍵詞，資料放 `Resources/lexicon.json`，可隨時擴充：

- **餐飲**：早餐、午餐、晚餐、宵夜、便當、飯、麵、水餃、火鍋、燒肉、拉麵、壽司、牛排、自助餐、早午餐、麵包、鹽酥雞、雞排、滷味、麥當勞、肯德基、摩斯、subway、超商、小七、7-11、全家、萊爾富、OK、咖啡、星巴克、路易莎、cama、手搖、飲料、清心、50嵐、五十嵐、可不可、迷客夏、大苑子、珍奶、奶茶、紅茶、綠茶
- **交通**：捷運、公車、客運、火車、台鐵、高鐵、悠遊卡、一卡通、加油、油錢、計程車、小黃、uber、停車、停車費、過路費、etag、ubike、車票、車錢
- **日用品**：衛生紙、洗髮精、沐浴乳、牙膏、牙刷、洗衣精、肥皂、刮鬍刀、毛巾、垃圾袋、電池、屈臣氏、康是美、寶雅、大創、全聯、家樂福、好市多、costco
- **娛樂**：電影、遊戲、課金、抽卡、steam、switch、ps5、netflix、spotify、youtube、訂閱、ktv、唱歌、酒、展覽、演唱會、球賽、桌遊、出遊、旅遊、住宿、飯店
- **治裝**：衣服、褲子、鞋、外套、襪子、內衣褲、帽子、皮帶、uniqlo、net、lativ、迪卡儂、剪頭髮、理髮、髮廊
- **醫療**：看醫生、門診、掛號、掛號費、藥、藥局、感冒、牙醫、眼科、診所、醫院、隱形眼鏡、藥膏、ok繃、維他命、保健食品
- **通訊**：電話費、手機費、網路費、儲值、預付卡、中華電信、遠傳、台哥大、亞太、月租
- **軍中場景**：福利社、合作社、營站、放假、收假、加菜金、寄物、洗衣、伙食
- **其他**：紅包、禮物、捐款、罰單、房租、水電、繳費

#### C. 情緒標籤詞
- 必要：必要、需要、一定要、不得不、必須、日常
- 衝動：衝動、亂買、手滑、忍不住、又買、後悔、剁手
- 享受：犒賞、獎勵、爽、享受、放鬆、慶祝

#### D. 日期詞
今天、昨天、前天、大前天、上禮拜X、禮拜X、星期X、X號、X月X日、早上、中午、下午、晚上、半夜

#### E. 模糊比對（四層，愈往下愈寬鬆）
1. **正規化**：全形→半形、大小寫統一、去空白、簡→繁、常見錯字表
2. **完全命中**：詞庫直接包含 → 最高分
3. **拼音命中**：用 iOS 內建的 `CFStringTransform(kCFStringTransformMandarinLatin)` 把輸入與詞庫都轉成拼音再比
   → 這一層專治同音字（「便當/變當/便噹」「捷運/借運/節運」「早餐/早参」全部能救）
4. **編輯距離**：對拼音字串算 Levenshtein，容許 1~2 個字差 → 治「講得含糊、少一個音」

每個命中都給分數，最高分者勝；**分數低於門檻時在確認畫面把該欄位標黃**，提醒「這欄我不太確定」。

#### F. 個人化學習（不用 AI，但很有效）
新增 SwiftData model `PhraseMapping { phrase, categoryID, hitCount, updatedAt }`：
- 使用者在確認畫面**手動改掉分類** → 把原句的關鍵片語與最終分類記起來
- 下次講同樣的話直接命中，而且優先權高於內建詞庫
- 「更多」頁可以看／刪這份個人詞庫

這等於讓 App 越用越懂你的講法，完全不需要 AI。

#### G. 一句多筆
以「然後、還有、再來、跟、和、加上、，」切句，每段各自解析，確認畫面用「第 1/3 筆」逐筆過。

#### H. 確認畫面加強
- 最上面顯示辨識到的原句，命中的詞用顏色標出來
- 每個欄位右邊小圖示表示「哪裡來的」：🎙 語音抓到 / ✋ 你改的 / ⚠️ 猜的
- 一鍵「重講」

### 驗收標準（做完要能過這些句子）
```
「早餐五十」              → 50 / 餐飲·早餐
「剛剛買便當一百二」       → 120 / 餐飲·午餐（或餐飲）
「加油七百塊」            → 700 / 交通
「昨天晚上鹽酥雞一百」     → 100 / 餐飲·宵夜 / 日期=昨天
「手搖飲六十衝動」         → 60 / 餐飲·飲料 / 情緒=衝動
「福利社買飲料三十五」     → 35 / 餐飲·飲料
「早餐五十然後咖啡六十」   → 兩筆
```

---

## 3. 其他建議功能（我的推薦，依 CP 值排序）

### ✅ v2.0 已完成

1. ✅ **台灣電子發票 QR 解析**（`InvoiceQRParser`，掃到就用 QR 的金額與日期，OCR 當備援）
2. ✅ **完整備份／還原**（`BackupService`，含收據照片選項，換手機用這個）
3. ✅ **每日結餘結轉**（首頁顯示「比進度省下 / 超前」，每日額度本來就會跟著剩餘天數重算）
4. ✅ **互動式 Widget**（中尺寸小工具上直接 +金額、一鍵語音）
5. ✅ **本地通知**（每日提醒 + 燒錢紅燈警告，時間可調）
6. ✅ **分類詳情頁**（報表點分類 → 六個月趨勢、子分類佔比、當月紀錄）
7. ✅ **Face ID / 密碼鎖**

### 🔥 原本的強烈建議清單（保留紀錄）

1. **台灣電子發票 QR 解析** — 收據 OCR 的最強升級
   用 `VNDetectBarcodesRequest` 掃發票上的 QR，左碼裡直接就有發票號碼、開立日期、金額（十六進位），
   比 OCR 猜「總計」準得多，幾乎 100% 命中。OCR 當備援。
   *（實作前要照財政部電子發票規格再核對一次欄位長度）*

2. **完整備份 / 還原（取代 iCloud 的角色）**
   一鍵匯出**包含收據照片**的 `.moneyleft` 備份檔（JSON + 圖片打包），存到「檔案」App 或 AirDrop 給自己；
   換手機時一鍵還原。拔掉 iCloud 之後這個一定要有，不然資料只存在一台裝置上很危險。

3. **每日結餘結轉**
   今天沒花完的額度自動累加到明天。非常貼合「今天還能花多少」的核心理念，
   而且會產生正回饋：今天忍住 → 明天額度變多。

4. **互動式 Widget（iOS 17）**
   桌面小工具上直接放 `+50 / +100 / 語音` 按鈕，不用開 App 就記完一筆。
   技術已經有了（動態島那套 AppIntent 可以直接複用）。

5. **本地通知**
   - 每天晚上 21:00 提醒「今天記了嗎」
   - 燒錢速度轉紅燈時提醒「照這速度 X 號就會超支」
   - 月初提醒設定新月預算 + 上月結算摘要

### 👍 值得做

6. **記帳範本／常用組合** — 「早餐 60」「機車加油 200」存成範本，一鍵記一筆
7. **定期支出自動入帳** — 房租、訂閱到扣款日自動產生一筆待確認
8. **分類詳情頁** — 點分類看該分類的月趨勢、所有紀錄、平均單價
9. **大額分期攤提** — 一筆 12000 分 12 個月攤進每月預算，不會讓某個月爆掉
10. **Face ID / 密碼鎖** — `LocalAuthentication`，打開 App 要驗證
11. **月報卡片分享** — 月底生成一張圖（總支出、分類佔比、情緒佔比），可存相簿
12. **搜尋強化** — 金額範圍、日期範圍、只看有收據的

### 🙂 有空再說

13. 年度回顧（你今年在餐飲花了多少、最貴的一筆）
14. 主題色自訂 / 深色模式微調
15. iPad 版面與橫向支援
16. 多幣別（出國用）
17. 匯出圖表成圖片
18. 無障礙與動態字體完整支援

---

## 4. 已知待辦（技術債）

- ✅ ~~`StatTile` 元件已無人使用，可刪~~（v2.0 已刪）
- `RootView` 的 `liveActivity` 目前沒直接用到，只為了傳 environment
- 收據批次辨識時沒有取消按鈕，長佇列只能等
- OCR 金額解析對「發票號碼被誤判成金額」的防護還可以再強（長度 ≥8 已排除，但可再加格式規則）
- 尚未在真機驗證：動態島按鈕、Siri 記帳、Widget 更新時機、語音辨識、發票 QR 解析
- 電子發票 QR 左碼欄位長度是照公開規格寫的，建議拿實際發票核對一次
- 語音辨識需要裝置先下載中文離線聽寫語言包，否則會顯示引導而不會改走網路

## 5. 刻意「不做」的項目（避免變成大雜燴）

| 項目 | 為什麼不做 |
|---|---|
| 記帳範本／常用組合 | 首頁快速分類 + 語音記帳已經比範本更快 |
| 定期支出自動入帳 | 固定支出已經從預算扣掉，再自動產生紀錄會變成重複計算 |
| 大額分期攤提 | 概念複雜、日常用不到，會讓預算數字更難懂 |
| 月報分享圖 / 年度回顧 | 好看但不影響決策，晚點再說 |
| 多幣別 | 目前只在台灣用 |
| 主題色自訂 | 換皮而已，先把功能做好 |
| iPad 專屬版面 | 主要使用情境是手機單手操作 |
